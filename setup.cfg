# Helper file to handle all configs

[coverage:run]
# .coveragerc to control coverage.py and pytest-cov
omit =
    # Omit the tests
    */tests/*
    # Omit generated versioneer
    bayesiantesting/_version.py

[flake8]
# Flake8, PyFlakes, etc
max-line-length = 119
ignore = E203, E266, E501, W503
select = B,C,E,F,W,T4,B9

[versioneer]
# Automatic version numbering scheme
VCS = git
style = pep440
versionfile_source = bayesiantesting/_version.py
versionfile_build = bayesiantesting/_version.py
tag_prefix = ''

[aliases]
test = pytest
