name: test
channels:
dependencies:
    # Base depends
  - python =3.7
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Main
  - numpy
  - scipy
  - pyyaml
  - pandas
  - matplotlib
  - pymbar
  - tqdm
  - statsmodels
  - pint
  - uncertainties

    # Pip-only installs
  #- pip:
  #  - codecov

