name: test
channels:
dependencies:
    # Base depends
  - python =3.7
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Main
  - numpy
  - scipy
  - yaml
  - pandas
  - matplotlib
  - pymbar
  - tqdm
  - statsmodels

    # Pip-only installs
  #- pip:
  #  - codecov

